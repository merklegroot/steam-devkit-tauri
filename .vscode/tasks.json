{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "npm: install",
      "type": "shell",
      "command": "npm install",
      "options": {
        "cwd": "${workspaceFolder}"
      }
    },
    {
      "label": "python: setup venv",
      "type": "shell",
      "command": "npm run setup:python",
      "options": {
        "cwd": "${workspaceFolder}"
      }
    }
  ]
}

